import adminStore from "@/store/adminStore"

export const metadata = {
  title: `${adminStore.pages.programs.title} | ${adminStore.title}`,
}

const AdminPrograms = () => {
  return (
    <>
      
    </>
  )
}

export default AdminPrograms