import adminStore from "@/store/adminStore"

export const metadata = {
  title: `${adminStore.pages.questions.title} | ${adminStore.title}`,
}

const AdminQuestions = () => {
  return (
    <>
      
    </>
  )
}

export default AdminQuestions